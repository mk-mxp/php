<?php
{# See all available values with `{{ dump() }}` #}
{# or "data" with `{{ data | json_encode(constant('JSON_PRETTY_PRINT')) }}` #}

declare(strict_types=1);

use PHPUnit\Framework\TestCase;
use PHPUnit\Framework\Attributes\TestDox;

final class LineUpTest extends TestCase
{
    public static function setUpBeforeClass(): void
    {
        require_once('LineUp.php');
    }
{% for case in data.cases %}

    /** uuid: {{ case.uuid }} */
     #[TestDox('{{ case.description }}')]
    public function {{ testfn(case.description) }}(): void
    {
        $this->assertSame(
            '{{ case.expected}}',
            format('{{ case.input.name }}', {{ case.input.number }})
        );
    }
{% endfor -%}
}
